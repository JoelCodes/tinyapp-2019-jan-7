<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>

  <body style="font-size:20px; padding:50px;">
    <header>
      <% include ./partials/_header %>
    </header>
      <p>ShortURL - <b><%= shortURL %></b></p>
      <p>
        <form method="POST" action="/urls/<%= shortURL %>?_method=PUT">
          <input type="text" name="newFull" placeholder="<%= longURL %>" style="width: 200px;">
          <button>Edit</button>
        </form>
      </p>
  </body>
</html>